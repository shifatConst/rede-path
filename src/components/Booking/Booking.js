import React, { useContext } from 'react';
import { RideContext } from '../Destination/Destination';

const Booking = () => {
    // const [ride, setRide] = useContext(RideContext);
    return (
        <div>
            <h1>This is booking page.</h1>
        </div>
    );
};

export default Booking;